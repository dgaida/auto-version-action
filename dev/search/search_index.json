{"config":{"lang":["de","en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Auto Version Action","text":"<p>Willkommen zur Dokumentation der Auto Version Action. Diese Sammlung von GitHub Actions automatisiert die Wartung deiner Repositories.</p>"},{"location":"#hauptfunktionen","title":"Hauptfunktionen","text":"<ul> <li>Auto Versioning: Inkrementiert die Version in <code>pyproject.toml</code> und erstellt Git-Tags.  </li> <li>Add Badges: Erkennt Repository-Features und f\u00fcgt automatisch Badges zur <code>README.md</code> hinzu.  </li> <li>Markdown List Fixer: Korrigiert Formatierungsfehler in Markdown-Listen f\u00fcr eine bessere Darstellung.  </li> </ul>"},{"location":"#schnellstart","title":"Schnellstart","text":"<pre><code>- name: Auto Version and Badges\n  uses: dgaida/auto-version-action@main\n  with:\n    github-token: ${{ secrets.GITHUB_TOKEN }}\n</code></pre>"},{"location":"configuration/","title":"Konfiguration","text":"<p>Die Action kann \u00fcber verschiedene Inputs angepasst werden.</p> Input Beschreibung Standardwert <code>github-token</code> GitHub Token f\u00fcr die Authentifizierung <code>${{ github.token }}</code> <code>auto-version</code> Ob die Version automatisch erh\u00f6ht werden soll <code>true</code> <code>create-badge</code> Ob Badges automatisch hinzugef\u00fcgt/aktualisiert werden sollen <code>true</code> <code>md-list-fix</code> Ob Markdown-Listen korrigiert werden sollen <code>true</code> <code>run-on-push</code> Ob die Action bei Push-Events (au\u00dferhalb von PRs) laufen soll <code>true</code>"},{"location":"getting-started/","title":"Erste Schritte","text":"<p>Diese Anleitung hilft dir dabei, die Auto Version Action in dein Repository zu integrieren.</p>"},{"location":"getting-started/#voraussetzungen","title":"Voraussetzungen","text":"<ul> <li>Ein Python-Projekt mit einer <code>pyproject.toml</code>.  </li> <li>GitHub Actions sind aktiviert.  </li> </ul>"},{"location":"getting-started/#einrichtung","title":"Einrichtung","text":"<ol> <li>Erstelle eine neue Workflow-Datei unter <code>.github/workflows/docs.yml</code>.  </li> <li>F\u00fcge die Action zu deinen Schritten hinzu:  </li> </ol> <pre><code>jobs:\n  maintenance:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: write\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - name: Auto Version and Badges\n        uses: dgaida/auto-version-action@main\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n</code></pre> <ol> <li>Committe und pushe die Datei. Die Action wird nun bei jedem Push auf den Haupt-Branch ausgef\u00fchrt.  </li> </ol>"},{"location":"installation/","title":"Installation","text":"<p>Die Auto Version Action muss nicht lokal installiert werden, da sie als GitHub Action konzipiert ist. F\u00fcr die lokale Entwicklung oder Tests der Skripte kannst du jedoch die Abh\u00e4ngigkeiten installieren.</p>"},{"location":"installation/#github-action-nutzen","title":"GitHub Action nutzen","text":"<p>F\u00fcge einfach den folgenden Schritt in deinen Workflow ein:</p> <pre><code>- uses: dgaida/auto-version-action@v0.1.8\n</code></pre>"},{"location":"installation/#lokale-entwicklung","title":"Lokale Entwicklung","text":"<p>Klonen des Repositories:</p> <pre><code>git clone https://github.com/dgaida/auto-version-action.git\ncd auto-version-action\n</code></pre> <p>Installation der Test-Abh\u00e4ngigkeiten:</p> <pre><code>pip install -e \".[test]\"\n</code></pre>"},{"location":"metrics/","title":"Dokumentations-Metriken","text":"<p>Diese Seite bietet einen \u00dcberblick \u00fcber die Qualit\u00e4t und Vollst\u00e4ndigkeit unserer Dokumentation.</p> <p>Lade Metriken...</p>"},{"location":"metrics/#badge-status","title":"Badge-Status","text":"<ul> <li>API-Abdeckung:  </li> </ul>"},{"location":"troubleshooting/","title":"Fehlerbehebung","text":"<p>Hier findest du L\u00f6sungen f\u00fcr h\u00e4ufig auftretende Probleme.</p>"},{"location":"troubleshooting/#permission-denied-beim-pushen","title":"\"Permission denied\" beim Pushen","text":"<p>Stelle sicher, dass dein Workflow \u00fcber Schreibberechtigungen verf\u00fcgt:</p> <pre><code>permissions:\n  contents: write\n</code></pre>"},{"location":"troubleshooting/#version-wird-nicht-erkannt","title":"Version wird nicht erkannt","text":"<p>Die Action sucht in der <code>pyproject.toml</code> nach dem Muster <code>version = \"X.Y.Z\"</code>. Stelle sicher, dass dieses Format eingehalten wird.</p>"},{"location":"troubleshooting/#badges-werden-nicht-hinzugefugt","title":"Badges werden nicht hinzugef\u00fcgt","text":"<p>Die Action f\u00fcgt Badges nach dem ersten Absatz oder in einem bestehenden Badge-Block hinzu. Wenn deine <code>README.md</code> leer ist oder ein ungew\u00f6hnliches Format hat, kann die Erkennung fehlschlagen.</p>"},{"location":"api/","title":"API-Referenz","text":"<p>Diese Seite enth\u00e4lt die automatisch generierte Dokumentation der Python-Skripte.</p>"},{"location":"api/#badge-erkennung-add_badgespy","title":"Badge-Erkennung (<code>add_badges.py</code>)","text":""},{"location":"api/#add_badges","title":"<code>add_badges</code>","text":"<p>Script to automatically detect and add badges to README.md.</p>"},{"location":"api/#add_badges-functions","title":"Functions","text":""},{"location":"api/#add_badges.detect_python_version","title":"<code>detect_python_version()</code>","text":"<p>Detects the required Python version from pyproject.toml.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str | None</code> <p>The Python version string (e.g., \"3.11+\") if found, otherwise None.</p> Source code in <code>add_badges.py</code> <pre><code>def detect_python_version() -&gt; str | None:\n    \"\"\"Detects the required Python version from pyproject.toml.\n\n    Returns:\n        str: The Python version string (e.g., \"3.11+\") if found, otherwise None.\n    \"\"\"\n    if os.path.exists(\"pyproject.toml\"):\n        try:\n            with open(\"pyproject.toml\", \"r\") as f:\n                content = f.read()\n                match = re.search(r'requires-python = \"(.*?)\"', content)\n                if match:\n                    v = match.group(1).replace(\"&gt;=\", \"\").replace(\"&gt;\", \"\").strip()\n                    return f\"{v}+\"\n        except Exception:\n            pass\n    return None\n</code></pre>"},{"location":"api/#add_badges.detect_version","title":"<code>detect_version()</code>","text":"<p>Detects the project version from pyproject.toml or package.json.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str | None</code> <p>The version string if found, otherwise None.</p> Source code in <code>add_badges.py</code> <pre><code>def detect_version() -&gt; str | None:\n    \"\"\"Detects the project version from pyproject.toml or package.json.\n\n    Returns:\n        str: The version string if found, otherwise None.\n    \"\"\"\n    if os.path.exists(\"pyproject.toml\"):\n        try:\n            with open(\"pyproject.toml\", \"r\") as f:\n                for line in f:\n                    if line.strip().startswith(\"version =\"):\n                        match = re.search(r'version = \"(.*?)\"', line)\n                        if match:\n                            return match.group(1)\n        except Exception:\n            pass\n    if os.path.exists(\"package.json\"):\n        try:\n            with open(\"package.json\", \"r\") as f:\n                data = json.load(f)\n                return data.get(\"version\")\n        except Exception:\n            pass\n    return None\n</code></pre>"},{"location":"api/#add_badges.get_applicable_badges","title":"<code>get_applicable_badges()</code>","text":"<p>Determines which badges should be added based on repo features.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list[str]</code> <p>A list of Markdown badge strings.</p> Source code in <code>add_badges.py</code> <pre><code>def get_applicable_badges() -&gt; list[str]:\n    \"\"\"Determines which badges should be added based on repo features.\n\n    Returns:\n        list: A list of Markdown badge strings.\n    \"\"\"\n    owner, name = get_repo_info()\n    branch = get_default_branch()\n    badges = []\n\n    if has_tags(owner, name):\n        badges.append(f\"[![Version](https://img.shields.io/github/v/tag/{owner}/{name}?label=version)](https://github.com/{owner}/{name}/tags)\")\n    else:\n        v = detect_version()\n        if v:\n            badges.append(f\"![Version](https://img.shields.io/badge/version-{v}-blue)\")\n\n    if is_python_repo():\n        pv = detect_python_version() or \"3.8+\"\n        badges.append(f\"[![Python](https://img.shields.io/badge/python-{pv}-blue.svg)](https://www.python.org/downloads/)\")\n\n    if has_mit_license():\n        badges.append(\"[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)\")\n\n    if uses_codecov():\n        badges.append(f\"[![codecov](https://codecov.io/gh/{owner}/{name}/branch/{branch}/graph/badge.svg)](https://codecov.io/gh/{owner}/{name})\")\n\n    if has_workflow(\"lint\"):\n        badges.append(f\"[![Code Quality](https://github.com/{owner}/{name}/actions/workflows/lint.yml/badge.svg)](https://github.com/{owner}/{name}/actions/workflows/lint.yml)\")\n\n    if has_workflow(\"tests\"):\n        badges.append(f\"[![Tests](https://github.com/{owner}/{name}/actions/workflows/tests.yml/badge.svg)](https://github.com/{owner}/{name}/actions/workflows/tests.yml)\")\n\n    if has_workflow(\"codeql\"):\n        badges.append(f\"[![CodeQL](https://github.com/{owner}/{name}/actions/workflows/codeql.yml/badge.svg)](https://github.com/{owner}/{name}/actions/workflows/codeql.yml)\")\n\n    if uses_black():\n        badges.append(\"[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)\")\n\n    if uses_ruff():\n        badges.append(\"[![Ruff](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/ruff/main/assets/badge/v2.json)](https://github.com/astral-sh/ruff)\")\n\n    if has_github_pages(owner, name):\n        badges.append(f\"[![Docs](https://img.shields.io/badge/docs-GitHub%20Pages-blue)](https://{owner}.github.io/{name}/)\")\n\n    badges.append(f\"[![Maintenance](https://img.shields.io/badge/Maintained%3F-yes-green.svg)](https://github.com/{owner}/{name}/graphs/commit-activity)\")\n    badges.append(f\"![Last commit](https://img.shields.io/github/last-commit/{owner}/{name})\")\n\n    return badges\n</code></pre>"},{"location":"api/#add_badges.get_default_branch","title":"<code>get_default_branch()</code>","text":"<p>Gets the default branch name from environment variables.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The default branch name (e.g., \"main\" or \"master\").</p> Source code in <code>add_badges.py</code> <pre><code>def get_default_branch() -&gt; str:\n    \"\"\"Gets the default branch name from environment variables.\n\n    Returns:\n        str: The default branch name (e.g., \"main\" or \"master\").\n    \"\"\"\n    return os.environ.get(\"GITHUB_REF_NAME\", \"master\")\n</code></pre>"},{"location":"api/#add_badges.get_repo_info","title":"<code>get_repo_info()</code>","text":"<p>Gets the repository owner and name from environment variables.</p> <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[str, str]</code> <p>(owner, name) as strings.</p> Source code in <code>add_badges.py</code> <pre><code>def get_repo_info() -&gt; tuple[str, str]:\n    \"\"\"Gets the repository owner and name from environment variables.\n\n    Returns:\n        tuple: (owner, name) as strings.\n    \"\"\"\n    full_repo = os.environ.get(\"GITHUB_REPOSITORY\")\n    if not full_repo or \"/\" not in full_repo:\n        return \"dgaida\", \"auto-version-action\"\n    owner, name = full_repo.split(\"/\")\n    return owner, name\n</code></pre>"},{"location":"api/#add_badges.has_github_pages","title":"<code>has_github_pages(owner, name)</code>","text":"<p>Checks if GitHub Pages or MkDocs is configured for the repo.</p> <p>Parameters:</p> Name Type Description Default <code>owner</code> <code>str</code> <p>Repository owner.</p> required <code>name</code> <code>str</code> <p>Repository name.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if docs are found or the page responds with 200.</p> Source code in <code>add_badges.py</code> <pre><code>def has_github_pages(owner, name) -&gt; bool:\n    \"\"\"Checks if GitHub Pages or MkDocs is configured for the repo.\n\n    Args:\n        owner (str): Repository owner.\n        name (str): Repository name.\n\n    Returns:\n        bool: True if docs are found or the page responds with 200.\n    \"\"\"\n    url = f\"https://{owner}.github.io/{name}/\"\n    try:\n        req = urllib.request.Request(url, method='HEAD')\n        with urllib.request.urlopen(req, timeout=2) as resp:\n            if resp.status == 200:\n                return True\n    except Exception:\n        pass\n    return os.path.exists(\"docs/\") or os.path.exists(\"mkdocs.yml\")\n</code></pre>"},{"location":"api/#add_badges.has_mit_license","title":"<code>has_mit_license()</code>","text":"<p>Checks if the repository has an MIT license file.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if an MIT license is found, False otherwise.</p> Source code in <code>add_badges.py</code> <pre><code>def has_mit_license() -&gt; bool:\n    \"\"\"Checks if the repository has an MIT license file.\n\n    Returns:\n        bool: True if an MIT license is found, False otherwise.\n    \"\"\"\n    for f in [\"LICENSE\", \"LICENSE.txt\", \"LICENSE.md\", \"license\"]:\n        if os.path.exists(f):\n            try:\n                with open(f, \"r\") as file:\n                    content = file.read()\n                    if \"MIT License\" in content or \"MIT LICENSE\" in content:\n                        return True\n            except Exception:\n                pass\n    return False\n</code></pre>"},{"location":"api/#add_badges.has_tags","title":"<code>has_tags(owner, name)</code>","text":"<p>Checks if the repository has any Git tags using the GitHub API.</p> <p>Parameters:</p> Name Type Description Default <code>owner</code> <code>str</code> <p>Repository owner.</p> required <code>name</code> <code>str</code> <p>Repository name.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if at least one tag exists.</p> Source code in <code>add_badges.py</code> <pre><code>def has_tags(owner, name) -&gt; bool:\n    \"\"\"Checks if the repository has any Git tags using the GitHub API.\n\n    Args:\n        owner (str): Repository owner.\n        name (str): Repository name.\n\n    Returns:\n        bool: True if at least one tag exists.\n    \"\"\"\n    token = os.environ.get(\"GITHUB_TOKEN\")\n    url = f\"https://api.github.com/repos/{owner}/{name}/tags\"\n    req = urllib.request.Request(url)\n    req.add_header(\"User-Agent\", \"Add-Badges-Action\")\n    if token:\n        req.add_header(\"Authorization\", f\"token {token}\")\n    try:\n        with urllib.request.urlopen(req, timeout=5) as response:\n            tags = json.loads(response.read().decode())\n            return isinstance(tags, list) and len(tags) &gt; 0\n    except Exception as e:\n        print(f\"Error checking tags for {owner}/{name}: {e}\")\n        return False\n</code></pre>"},{"location":"api/#add_badges.has_workflow","title":"<code>has_workflow(wf_name)</code>","text":"<p>Checks if a specific GitHub workflow exists.</p> <p>Parameters:</p> Name Type Description Default <code>wf_name</code> <code>str</code> <p>The name of the workflow file (without extension).</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the workflow exists, False otherwise.</p> Source code in <code>add_badges.py</code> <pre><code>def has_workflow(wf_name) -&gt; bool:\n    \"\"\"Checks if a specific GitHub workflow exists.\n\n    Args:\n        wf_name (str): The name of the workflow file (without extension).\n\n    Returns:\n        bool: True if the workflow exists, False otherwise.\n    \"\"\"\n    return os.path.exists(f\".github/workflows/{wf_name}.yml\") or os.path.exists(f\".github/workflows/{wf_name}.yaml\")\n</code></pre>"},{"location":"api/#add_badges.is_python_repo","title":"<code>is_python_repo()</code>","text":"<p>Checks if the repository is a Python project.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if Python-specific files exist, False otherwise.</p> Source code in <code>add_badges.py</code> <pre><code>def is_python_repo() -&gt; bool:\n    \"\"\"Checks if the repository is a Python project.\n\n    Returns:\n        bool: True if Python-specific files exist, False otherwise.\n    \"\"\"\n    return any(os.path.exists(f) for f in [\"pyproject.toml\", \"setup.py\", \"requirements.txt\"])\n</code></pre>"},{"location":"api/#add_badges.update_readme","title":"<code>update_readme()</code>","text":"<p>Updates the README.md file with detected badges.</p> <p>The function scans the repository for features, generates badges, and inserts or updates them in the README.md.</p> Source code in <code>add_badges.py</code> <pre><code>def update_readme():\n    \"\"\"Updates the README.md file with detected badges.\n\n    The function scans the repository for features, generates badges,\n    and inserts or updates them in the README.md.\n    \"\"\"\n    filepath = \"README.md\"\n    if not os.path.exists(filepath):\n        print(f\"{filepath} not found\")\n        return\n\n    available_badges = get_applicable_badges()\n\n    with open(filepath, \"r\") as f:\n        content = f.read()\n\n    original_content = content\n    # Replacement logic for version badge\n    version_pattern = re.compile(r'(!\\[Version\\]\\(https://img\\.shields\\.io/badge/version-.*?\\)|\\[!\\[Version\\]\\(https://img\\.shields\\.io/github/v/tag/.*?\\)\\]\\(.*?\\))')\n    new_version_badge = next((b for b in available_badges if \"![Version]\" in b), None)\n    if new_version_badge and version_pattern.search(content):\n        content = version_pattern.sub(new_version_badge, content)\n\n    missing_badges = []\n    for badge in available_badges:\n        match = re.search(r'\\!\\[.*?\\]\\((.*?)\\)', badge)\n        if not match:\n            continue\n        img_url = match.group(1)\n\n        # Determine unique part of the URL to check for existence\n        unique_part = img_url\n        if \"img.shields.io/badge/version-\" in img_url:\n            unique_part = \"img.shields.io/badge/version-\"\n        elif \"img.shields.io/badge/python-\" in img_url:\n            unique_part = \"img.shields.io/badge/python-\"\n        elif \"img.shields.io/badge/License-MIT\" in img_url:\n            unique_part = \"img.shields.io/badge/License-MIT\"\n        elif \"last-commit\" in img_url:\n            unique_part = \"img.shields.io/github/last-commit\"\n\n        if unique_part not in content:\n            missing_badges.append(badge)\n\n    if not missing_badges and content == original_content:\n        print(\"No missing badges to add and no updates needed.\")\n        return\n\n    lines = content.splitlines()\n    badge_pattern = re.compile(r'\\!\\[.*?\\]\\(.*?\\)')\n    badge_lines = [i for i, line in enumerate(lines) if badge_pattern.search(line)]\n\n    if badge_lines:\n        block_end = badge_lines[0]\n        for i in range(1, len(badge_lines)):\n            if badge_lines[i] == badge_lines[i-1] + 1:\n                block_end = badge_lines[i]\n            else:\n                break\n        # Insert them on separate lines\n        new_lines = lines[:block_end+1] + missing_badges + lines[block_end+1:]\n        lines = new_lines\n    else:\n        i = 0\n        while i &lt; len(lines) and (lines[i].startswith('#') or not lines[i].strip()):\n            i += 1\n        # Now i is at start of first paragraph\n        while i &lt; len(lines) and lines[i].strip():\n            i += 1\n        # Now i is at blank line after first paragraph or end\n        lines = lines[:i] + [\"\"] + missing_badges + [\"\"] + lines[i:]\n\n    with open(filepath, \"w\") as f:\n        f.write(\"\\n\".join(lines) + \"\\n\")\n\n    messages = []\n    if missing_badges:\n        messages.append(f\"Added {len(missing_badges)} badges\")\n    if content != original_content:\n        messages.append(\"updated existing badges\")\n    if messages:\n        print(\". \".join(messages).capitalize() + \".\")\n</code></pre>"},{"location":"api/#add_badges.uses_black","title":"<code>uses_black()</code>","text":"<p>Checks if the project uses the Black code formatter.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if Black is configured in pyproject.toml.</p> Source code in <code>add_badges.py</code> <pre><code>def uses_black() -&gt; bool:\n    \"\"\"Checks if the project uses the Black code formatter.\n\n    Returns:\n        bool: True if Black is configured in pyproject.toml.\n    \"\"\"\n    if os.path.exists(\"pyproject.toml\"):\n        try:\n            with open(\"pyproject.toml\", \"r\") as f:\n                if \"[tool.black]\" in f.read():\n                    return True\n        except Exception:\n            pass\n    return False\n</code></pre>"},{"location":"api/#add_badges.uses_codecov","title":"<code>uses_codecov()</code>","text":"<p>Checks if the repository uses Codecov.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if Codecov configuration or workflow is found.</p> Source code in <code>add_badges.py</code> <pre><code>def uses_codecov() -&gt; bool:\n    \"\"\"Checks if the repository uses Codecov.\n\n    Returns:\n        bool: True if Codecov configuration or workflow is found.\n    \"\"\"\n    if os.path.exists(\".codecov.yml\") or os.path.exists(\"codecov.yml\"):\n        return True\n    if os.path.exists(\".github/workflows\"):\n        try:\n            for f in os.listdir(\".github/workflows\"):\n                if f.endswith((\".yml\", \".yaml\")):\n                    with open(os.path.join(\".github/workflows\", f), \"r\") as file:\n                        if \"codecov/codecov-action\" in file.read():\n                            return True\n        except Exception:\n            pass\n    return False\n</code></pre>"},{"location":"api/#add_badges.uses_ruff","title":"<code>uses_ruff()</code>","text":"<p>Checks if the project uses the Ruff linter/formatter.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if Ruff is configured or used.</p> Source code in <code>add_badges.py</code> <pre><code>def uses_ruff() -&gt; bool:\n    \"\"\"Checks if the project uses the Ruff linter/formatter.\n\n    Returns:\n        bool: True if Ruff is configured or used.\n    \"\"\"\n    if os.path.exists(\"pyproject.toml\"):\n        try:\n            with open(\"pyproject.toml\", \"r\") as f:\n                if \"[tool.ruff]\" in f.read():\n                    return True\n        except Exception:\n            pass\n    return os.path.exists(\"ruff.toml\") or os.path.exists(\".ruff.toml\")\n</code></pre>"},{"location":"api/#versionierung-increment_versionpy","title":"Versionierung (<code>increment_version.py</code>)","text":""},{"location":"api/#auto-version.increment_version","title":"<code>auto-version.increment_version</code>","text":"<p>Script to automatically increment the version in pyproject.toml.</p>"},{"location":"api/#auto-version.increment_version-functions","title":"Functions","text":""},{"location":"api/#auto-version.increment_version.increment_version","title":"<code>increment_version()</code>","text":"<p>Increments the patch version in pyproject.toml.</p> <p>If the patch version reaches 10, it overflows to minor. If the minor reaches 10, it overflows to major.</p> <p>Raises:</p> Type Description <code>SystemExit</code> <p>If pyproject.toml is missing or version not found.</p> Source code in <code>auto-version/increment_version.py</code> <pre><code>def increment_version():\n    \"\"\"Increments the patch version in pyproject.toml.\n\n    If the patch version reaches 10, it overflows to minor.\n    If the minor reaches 10, it overflows to major.\n\n    Raises:\n        SystemExit: If pyproject.toml is missing or version not found.\n    \"\"\"\n    filepath = \"pyproject.toml\"\n    if not os.path.exists(filepath):\n        print(f\"Error: {filepath} not found in the root of the repository.\")\n        sys.exit(1)\n\n    with open(filepath) as f:\n        lines = f.readlines()\n\n    new_lines = []\n    found = False\n    for line in lines:\n        if not found and line.strip().startswith('version = \"'):\n            match = re.search(r'version = \"(\\d+)\\.(\\d+)\\.(\\d+)\"', line)\n            if match:\n                major, minor, patch = map(int, match.groups())\n                old_version = f\"{major}.{minor}.{patch}\"\n\n                patch += 1\n                if patch &gt; 9:\n                    patch = 0\n                    minor += 1\n                if minor &gt; 9:\n                    minor = 0\n                    major += 1\n\n                new_version = f\"{major}.{minor}.{patch}\"\n                line = line.replace(f'version = \"{old_version}\"', f'version = \"{new_version}\"')\n                found = True\n                print(f\"Incrementing version: {old_version} -&gt; {new_version}\")\n        new_lines.append(line)\n\n    if found:\n        with open(filepath, \"w\") as f:\n            f.writelines(new_lines)\n    else:\n        print(\"Error: Could not find version string in pyproject.toml\")\n        sys.exit(1)\n</code></pre>"},{"location":"api/#markdown-fixer-fix_md_listspy","title":"Markdown Fixer (<code>fix_md_lists.py</code>)","text":""},{"location":"api/#md-list-fixer.fix_md_lists","title":"<code>md-list-fixer.fix_md_lists</code>","text":"<p>Script to automatically fix Markdown list formatting.</p>"},{"location":"api/#md-list-fixer.fix_md_lists-functions","title":"Functions","text":""},{"location":"api/#md-list-fixer.fix_md_lists.fix_markdown_file","title":"<code>fix_markdown_file(filepath)</code>","text":"<p>Ensures Markdown list items end with two spaces for line breaks.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to the Markdown file.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the file was modified, False otherwise.</p> Source code in <code>md-list-fixer/fix_md_lists.py</code> <pre><code>def fix_markdown_file(filepath) -&gt; bool:\n    \"\"\"Ensures Markdown list items end with two spaces for line breaks.\n\n    Args:\n        filepath (str): Path to the Markdown file.\n\n    Returns:\n        bool: True if the file was modified, False otherwise.\n    \"\"\"\n    with open(filepath, 'r', encoding='utf-8') as f:\n        lines = f.readlines()\n\n    modified = False\n    new_lines = []\n    in_code_block = False\n\n    # Regex for list items: -, *, + or 1. 2. etc.\n    list_item_regex = re.compile(r'^\\s*([-*+]|\\d+\\.)\\s+')\n\n    for i in range(len(lines)):\n        line = lines[i]\n        stripped_line = line.strip()\n\n        # Toggle code block\n        if stripped_line.startswith('```'):\n            in_code_block = not in_code_block\n            new_lines.append(line)\n            continue\n\n        if in_code_block:\n            new_lines.append(line)\n            continue\n\n        # Check if current line is a list item\n        is_list_item = bool(list_item_regex.match(line))\n\n        # Check if the next line is a list item (to handle the \"Liste:\" intro line)\n        is_next_line_list_item = False\n        if i + 1 &lt; len(lines):\n            is_next_line_list_item = bool(list_item_regex.match(lines[i+1]))\n\n        # We only process non-empty lines that are either list items or preceding a list item\n        if (is_list_item or is_next_line_list_item) and stripped_line:\n            # Determine line ending\n            current_newline = \"\"\n            if line.endswith('\\r\\n'):\n                current_newline = '\\r\\n'\n            elif line.endswith('\\n'):\n                current_newline = '\\n'\n\n            # Remove all trailing spaces and then add exactly two\n            content = line.rstrip('\\r\\n').rstrip(' ')\n\n            new_line = content + \"  \" + current_newline\n            if new_line != line:\n                modified = True\n                new_lines.append(new_line)\n            else:\n                new_lines.append(line)\n        else:\n            new_lines.append(line)\n\n    if modified:\n        with open(filepath, 'w', encoding='utf-8') as f:\n            f.writelines(new_lines)\n        return True\n    return False\n</code></pre>"},{"location":"api/#md-list-fixer.fix_md_lists.main","title":"<code>main()</code>","text":"<p>Main entry point for the Markdown list fixer action.</p> <p>Scans the repository for all .md files and applies fixes.</p> Source code in <code>md-list-fixer/fix_md_lists.py</code> <pre><code>def main():\n    \"\"\"Main entry point for the Markdown list fixer action.\n\n    Scans the repository for all .md files and applies fixes.\n    \"\"\"\n    files_to_check = []\n    for root, dirs, files in os.walk('.'):\n        # Skip .git and other hidden dirs\n        dirs[:] = [d for d in dirs if not d.startswith('.')]\n        for file in files:\n            if file.endswith('.md'):\n                files_to_check.append(os.path.join(root, file))\n\n    for filepath in files_to_check:\n        try:\n            if fix_markdown_file(filepath):\n                print(f\"Fixed {filepath}\")\n        except Exception as e:\n            print(f\"Error processing {filepath}: {e}\")\n</code></pre>"},{"location":"architecture/","title":"Architektur","text":"<p>Dieses Dokument beschreibt den Aufbau und den Datenfluss der Auto Version Action.</p>"},{"location":"architecture/#systemubersicht","title":"System\u00fcbersicht","text":"<p>Die Action besteht aus drei Hauptkomponenten, die nacheinander ausgef\u00fchrt werden.</p> graph TD     A[GitHub Event] --&gt; B{Auto Version Action}     B --&gt; C[increment_version.py]     B --&gt; D[fix_md_lists.py]     B --&gt; E[add_badges.py]     C --&gt; F[pyproject.toml aktualisiert]     D --&gt; G[Markdown-Dateien korrigiert]     E --&gt; H[README.md mit Badges aktualisiert]     F &amp; G &amp; H --&gt; I[Git Commit &amp; Push]     I --&gt; J[Git Tag Erstellung]"},{"location":"architecture/#datenfluss","title":"Datenfluss","text":"<p>Der Datenfluss konzentriert sich auf die Analyse des Repository-Status und die anschlie\u00dfende Modifikation von Metadaten und Dokumentation.</p> sequenceDiagram     participant GH as GitHub Workflow     participant IV as increment_version.py     participant FB as fix_md_lists.py     participant AB as add_badges.py     participant Git as Git Commands      GH-&gt;&gt;IV: Starte Versionierung     IV-&gt;&gt;IV: Lese pyproject.toml     IV--&gt;&gt;GH: Version erh\u00f6ht     GH-&gt;&gt;FB: Starte Markdown Fixer     FB-&gt;&gt;FB: Scanne .md Dateien     FB--&gt;&gt;GH: Listen korrigiert     GH-&gt;&gt;AB: Starte Badge-Erkennung     AB-&gt;&gt;AB: Analysiere Repository     AB--&gt;&gt;GH: README aktualisiert     GH-&gt;&gt;Git: Commit, Push &amp; Tag"},{"location":"development/","title":"Entwicklung","text":"<p>Willkommen im Entwicklungsbereich. Hier erf\u00e4hrst du, wie du zum Projekt beitragen kannst.</p>"},{"location":"development/#lokales-setup","title":"Lokales Setup","text":"<ol> <li>Repository klonen.  </li> <li>Virtuelle Umgebung erstellen: <code>python -m venv venv</code>.  </li> <li>Abh\u00e4ngigkeiten installieren: <code>pip install -e \".[test]\"</code>.  </li> </ol>"},{"location":"development/#tests-ausfuhren","title":"Tests ausf\u00fchren","text":"<p>Wir verwenden <code>pytest</code> f\u00fcr unsere Unit-Tests:</p> <pre><code>python -m pytest\n</code></pre>"},{"location":"development/#code-stil","title":"Code-Stil","text":"<p>Wir verwenden <code>ruff</code> f\u00fcr Linting und Formatierung. Bitte stelle sicher, dass dein Code den Standards entspricht.</p>"},{"location":"development/changelog/","title":"Changelog-Arbeitsablauf","text":"<p>Wir verwenden <code>git-cliff</code>, um unser Changelog automatisch aus Conventional Commits zu generieren.</p>"},{"location":"development/changelog/#konfiguration","title":"Konfiguration","text":"<p>Die Konfiguration befindet sich in <code>cliff.toml</code>. Sie gruppiert Commits in Kategorien wie \"Features\", \"Bug Fixes\" und \"Documentation\".</p>"},{"location":"development/changelog/#beitragen","title":"Beitragen","text":"<p>Bitte achte darauf, dass deine Commit-Nachrichten dem Conventional Commits Standard entsprechen:</p> <ul> <li><code>feat: ...</code> f\u00fcr neue Funktionen  </li> <li><code>fix: ...</code> f\u00fcr Fehlerbehebungen  </li> <li><code>docs: ...</code> f\u00fcr Dokumentations\u00e4nderungen  </li> </ul>"},{"location":"development/docstring-guide/","title":"Docstring-Leitfaden","text":"<p>Wir verwenden den Google-Stil f\u00fcr alle Python-Docstrings. Dies erm\u00f6glicht eine saubere, lesbare Dokumentation, die automatisch von <code>mkdocstrings</code> extrahiert werden kann.</p>"},{"location":"development/docstring-guide/#beispiel","title":"Beispiel","text":"<pre><code>def increment_version():\n    \"\"\"Inkrementiert die Patch-Version in der pyproject.toml.\n\n    Wenn die Patch-Version 10 erreicht, wird die Minor-Version erh\u00f6ht.\n    Wenn die Minor-Version 10 erreicht, wird die Major-Version erh\u00f6ht.\n\n    Raises:\n        SystemExit: Wenn die pyproject.toml fehlt oder die Version nicht gefunden wurde.\n    \"\"\"\n</code></pre>"},{"location":"development/docstring-guide/#abschnitte","title":"Abschnitte","text":"<ul> <li>Args: Beschreibung der Parameter.  </li> <li>Returns: Beschreibung des R\u00fcckgabewerts.  </li> <li>Raises: Dokumentation der Fehler, die die Funktion ausl\u00f6sen kann.  </li> <li>Example: Anwendungsbeispiele.  </li> </ul>"},{"location":"development/versioning/","title":"Versionierung und Releases","text":"<p>Dieses Projekt verwendet Semantic Versioning und automatisiert den Release-Prozess.</p>"},{"location":"development/versioning/#workflow","title":"Workflow","text":"<ol> <li>\u00c4nderungen committen: Verwende Conventional Commits.  </li> <li>Automatisches Inkrement: Bei jedem Push auf <code>main</code> erh\u00f6ht die Action die Patch-Version in der <code>pyproject.toml</code>.  </li> <li>Tag-Erstellung: Die Action erstellt automatisch einen Git-Tag (z.B. <code>v0.1.9</code>).  </li> <li>Changelog &amp; Release: Der neue Tag l\u00f6st den <code>release.yml</code> Workflow aus, der das Changelog mittels <code>git-cliff</code> aktualisiert und ein GitHub Release erstellt.  </li> </ol>"},{"location":"development/versioning/#manuelles-eingreifen","title":"Manuelles Eingreifen","text":"<p>Sollte eine Major- oder Minor-Version ben\u00f6tigt werden, muss diese manuell in der <code>pyproject.toml</code> angepasst werden. Die Action erkennt die \u00c4nderung und erstellt den entsprechenden Tag.</p>"},{"location":"usage/","title":"Verwendung","text":"<p>In diesem Abschnitt findest du detaillierte Beispiele f\u00fcr die Verwendung der verschiedenen Komponenten.</p>"},{"location":"usage/#nur-badges-hinzufugen","title":"Nur Badges hinzuf\u00fcgen","text":"<p>Wenn du nur die automatische Badge-Erkennung nutzen m\u00f6chtest:</p> <pre><code>- name: Add Badges\n  uses: dgaida/auto-version-action@main\n  with:\n    auto-version: false\n    create-badge: true\n</code></pre>"},{"location":"usage/#nur-versionierung","title":"Nur Versionierung","text":"<p>Wenn du nur die automatische Versionierung nutzen m\u00f6chtest:</p> <pre><code>- name: Auto Versioning\n  uses: dgaida/auto-version-action@main\n  with:\n    auto-version: true\n    create-badge: false\n</code></pre>"},{"location":"usage/#markdown-listen-korrigieren","title":"Markdown-Listen korrigieren","text":"<pre><code>- name: Markdown List Fixer\n  uses: dgaida/auto-version-action@main\n  with:\n    auto-version: false\n    create-badge: false\n    md-list-fix: true\n</code></pre>"},{"location":"en/","title":"Auto Version Action","text":"<p>Welcome to the Auto Version Action documentation. This collection of GitHub Actions automates your repository maintenance.</p>"},{"location":"en/#key-features","title":"Key Features","text":"<ul> <li>Auto Versioning: Increments the version in <code>pyproject.toml</code> and creates Git tags.  </li> <li>Add Badges: Detects repository features and automatically adds badges to <code>README.md</code>.  </li> <li>Markdown List Fixer: Fixes formatting errors in Markdown lists for better rendering.  </li> </ul>"},{"location":"en/#quick-start","title":"Quick Start","text":"<pre><code>- name: Auto Version and Badges\n  uses: dgaida/auto-version-action@main\n  with:\n    github-token: ${{ secrets.GITHUB_TOKEN }}\n</code></pre>"},{"location":"en/configuration/","title":"Configuration","text":"<p>The action can be customized via various inputs.</p> Input Description Default <code>github-token</code> GitHub token for authentication <code>${{ github.token }}</code> <code>auto-version</code> Whether to automatically increment the version <code>true</code> <code>create-badge</code> Whether to automatically add/update badges <code>true</code> <code>md-list-fix</code> Whether to automatically fix Markdown lists <code>true</code> <code>run-on-push</code> Whether to run on push events (outside of PRs) <code>true</code>"},{"location":"en/getting-started/","title":"Getting Started","text":"<p>This guide will help you integrate the Auto Version Action into your repository.</p>"},{"location":"en/getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>A Python project with a <code>pyproject.toml</code>.  </li> <li>GitHub Actions are enabled.  </li> </ul>"},{"location":"en/getting-started/#setup","title":"Setup","text":"<ol> <li>Create a new workflow file at <code>.github/workflows/docs.yml</code>.  </li> <li>Add the action to your steps:  </li> </ol> <pre><code>jobs:\n  maintenance:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: write\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - name: Auto Version and Badges\n        uses: dgaida/auto-version-action@main\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n</code></pre> <ol> <li>Commit and push the file. The action will now run on every push to the main branch.  </li> </ol>"},{"location":"en/installation/","title":"Installation","text":"<p>The Auto Version Action does not need to be installed locally as it is designed as a GitHub Action. However, for local development or testing of the scripts, you can install the dependencies.</p>"},{"location":"en/installation/#using-github-action","title":"Using GitHub Action","text":"<p>Simply add the following step to your workflow:</p> <pre><code>- uses: dgaida/auto-version-action@v0.1.8\n</code></pre>"},{"location":"en/installation/#local-development","title":"Local Development","text":"<p>Clone the repository:</p> <pre><code>git clone https://github.com/dgaida/auto-version-action.git\ncd auto-version-action\n</code></pre> <p>Install test dependencies:</p> <pre><code>pip install -e \".[test]\"\n</code></pre>"},{"location":"en/metrics/","title":"Documentation Metrics","text":"<p>This page provides an overview of the quality and completeness of our documentation.</p> <p>Loading metrics...</p>"},{"location":"en/metrics/#badge-status","title":"Badge Status","text":"<ul> <li>API Coverage:  </li> </ul>"},{"location":"en/troubleshooting/","title":"Troubleshooting","text":"<p>Here you will find solutions to common problems.</p>"},{"location":"en/troubleshooting/#permission-denied-during-push","title":"\"Permission denied\" during push","text":"<p>Ensure that your workflow has write permissions:</p> <pre><code>permissions:\n  contents: write\n</code></pre>"},{"location":"en/troubleshooting/#version-is-not-detected","title":"Version is not detected","text":"<p>The action looks for the pattern <code>version = \"X.Y.Z\"</code> in <code>pyproject.toml</code>. Ensure this format is followed.</p>"},{"location":"en/troubleshooting/#badges-are-not-added","title":"Badges are not added","text":"<p>The action adds badges after the first paragraph or in an existing badge block. If your <code>README.md</code> is empty or has an unusual format, detection might fail.</p>"},{"location":"en/api/","title":"API Reference","text":"<p>This page contains the automatically generated documentation for the Python scripts.</p>"},{"location":"en/api/#badge-detection-add_badgespy","title":"Badge Detection (<code>add_badges.py</code>)","text":""},{"location":"en/api/#versioning-increment_versionpy","title":"Versioning (<code>increment_version.py</code>)","text":""},{"location":"en/architecture/","title":"Architecture","text":"<p>This document describes the structure and data flow of the Auto Version Action.</p>"},{"location":"en/architecture/#system-overview","title":"System Overview","text":"<p>The action consists of three main components executed sequentially.</p> graph TD     A[GitHub Event] --&gt; B{Auto Version Action}     B --&gt; C[increment_version.py]     B --&gt; D[fix_md_lists.py]     B --&gt; E[add_badges.py]     C --&gt; F[pyproject.toml updated]     D --&gt; G[Markdown files fixed]     E --&gt; H[README.md updated with badges]     F &amp; G &amp; H --&gt; I[Git Commit &amp; Push]     I --&gt; J[Git Tag creation]"},{"location":"en/architecture/#data-flow","title":"Data Flow","text":"<p>The data flow focuses on analyzing the repository status and subsequently modifying metadata and documentation.</p> sequenceDiagram     participant GH as GitHub Workflow     participant IV as increment_version.py     participant FB as fix_md_lists.py     participant AB as add_badges.py     participant Git as Git Commands      GH-&gt;&gt;IV: Start Versioning     IV-&gt;&gt;IV: Read pyproject.toml     IV--&gt;&gt;GH: Version incremented     GH-&gt;&gt;FB: Start Markdown Fixer     FB-&gt;&gt;FB: Scan .md files     FB--&gt;&gt;GH: Lists fixed     GH-&gt;&gt;AB: Start Badge Detection     AB-&gt;&gt;AB: Analyze Repository     AB--&gt;&gt;GH: README updated     GH-&gt;&gt;Git: Commit, Push &amp; Tag"},{"location":"en/development/","title":"Development","text":"<p>Welcome to the development area. Here you can learn how to contribute to the project.</p>"},{"location":"en/development/#local-setup","title":"Local Setup","text":"<ol> <li>Clone the repository.  </li> <li>Create a virtual environment: <code>python -m venv venv</code>.  </li> <li>Install dependencies: <code>pip install -e \".[test]\"</code>.  </li> </ol>"},{"location":"en/development/#running-tests","title":"Running Tests","text":"<p>We use <code>pytest</code> for our unit tests:</p> <pre><code>python -m pytest\n</code></pre>"},{"location":"en/development/#code-style","title":"Code Style","text":"<p>We use <code>ruff</code> for linting and formatting. Please ensure your code meets the standards.</p>"},{"location":"en/development/changelog/","title":"Changelog Workflow","text":"<p>We use <code>git-cliff</code> to automatically generate our changelog from conventional commits.</p>"},{"location":"en/development/changelog/#configuration","title":"Configuration","text":"<p>The configuration is located in <code>cliff.toml</code>. It groups commits into categories like \"Features\", \"Bug Fixes\", and \"Documentation\".</p>"},{"location":"en/development/changelog/#contributing","title":"Contributing","text":"<p>Please ensure your commit messages follow the Conventional Commits standard:</p> <ul> <li><code>feat: ...</code> for new features  </li> <li><code>fix: ...</code> for bug fixes  </li> <li><code>docs: ...</code> for documentation changes  </li> </ul>"},{"location":"en/development/docstring-guide/","title":"Docstring Guide","text":"<p>We use the Google style for all Python docstrings. This allows for clean, readable documentation that can be automatically extracted by <code>mkdocstrings</code>.</p>"},{"location":"en/development/docstring-guide/#example","title":"Example","text":"<pre><code>def increment_version():\n    \"\"\"Increments the patch version in pyproject.toml.\n\n    If the patch version reaches 10, it overflows to minor.\n    If the minor reaches 10, it overflows to major.\n\n    Raises:\n        SystemExit: If pyproject.toml is missing or version not found.\n    \"\"\"\n</code></pre>"},{"location":"en/development/docstring-guide/#sections","title":"Sections","text":"<ul> <li>Args: Description of parameters.  </li> <li>Returns: Description of the return value.  </li> <li>Raises: Documentation of exceptions the function might raise.  </li> <li>Example: Usage examples.  </li> </ul>"},{"location":"en/development/versioning/","title":"Versioning and Releases","text":"<p>This project uses Semantic Versioning and automates the release process.</p>"},{"location":"en/development/versioning/#workflow","title":"Workflow","text":"<ol> <li>Commit Changes: Use Conventional Commits.  </li> <li>Automatic Increment: On every push to <code>main</code>, the action increments the patch version in <code>pyproject.toml</code>.  </li> <li>Tag Creation: The action automatically creates a Git tag (e.g., <code>v0.1.9</code>).  </li> <li>Changelog &amp; Release: The new tag triggers the <code>release.yml</code> workflow, which updates the changelog using <code>git-cliff</code> and creates a GitHub Release.  </li> </ol>"},{"location":"en/development/versioning/#manual-intervention","title":"Manual Intervention","text":"<p>If a major or minor version is required, it must be manually adjusted in <code>pyproject.toml</code>. The action will detect the change and create the corresponding tag.</p>"},{"location":"en/usage/","title":"Usage","text":"<p>In this section, you will find detailed examples of how to use the various components.</p>"},{"location":"en/usage/#add-badges-only","title":"Add Badges Only","text":"<p>If you only want to use the automatic badge detection:</p> <pre><code>- name: Add Badges\n  uses: dgaida/auto-version-action@main\n  with:\n    auto-version: false\n    create-badge: true\n</code></pre>"},{"location":"en/usage/#versioning-only","title":"Versioning Only","text":"<p>If you only want to use the automatic versioning:</p> <pre><code>- name: Auto Versioning\n  uses: dgaida/auto-version-action@main\n  with:\n    auto-version: true\n    create-badge: false\n</code></pre>"},{"location":"en/usage/#fix-markdown-lists","title":"Fix Markdown Lists","text":"<pre><code>- name: Markdown List Fixer\n  uses: dgaida/auto-version-action@main\n  with:\n    auto-version: false\n    create-badge: false\n    md-list-fix: true\n</code></pre>"}]}