name: 'Auto Versioning Action'
description: 'Increments version in pyproject.toml or package.json and creates a git tag'
author: 'Daniel Gaida'
inputs:
  run-on-push:
    description: 'Whether to run on push events (outside of PRs)'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Increment version
      if: github.event_name != 'push' || inputs.run-on-push == 'true'
      run: python ${{ github.action_path }}/increment_version.py
      shell: bash

    - name: Commit and push changes
      if: github.event_name != 'push' || inputs.run-on-push == 'true'
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"

        if [ -f "pyproject.toml" ]; then
          git add pyproject.toml
        elif [ -f "package.json" ]; then
          git add package.json
        fi

        git commit -m "chore: increment version [skip ci]"
        git pull --rebase origin ${{ github.event.pull_request.base.ref || github.ref_name }}
        git push origin ${{ github.event.pull_request.base.ref || github.ref_name }}
        
        # Extract new version and create tag
        NEW_VERSION=""
        if [ -f "pyproject.toml" ]; then
          NEW_VERSION=$(python3 -c 'import tomllib; print(tomllib.load(open("pyproject.toml", "rb"))["project"]["version"])')
        elif [ -f "package.json" ]; then
          NEW_VERSION=$(python3 -c 'import json; print(json.load(open("package.json"))["version"])')
        fi

        if [ -n "$NEW_VERSION" ]; then
          git tag v$NEW_VERSION
          git push origin v$NEW_VERSION
        fi
      shell: bash
